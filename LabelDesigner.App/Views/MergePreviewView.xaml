<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LabelDesigner.App.Views.MergePreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource AppSurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <canvas:CanvasControl x:Name="Canvas"
                              Grid.Row="0"
                              Draw="OnDraw"
                              ClearColor="White"/>
                              
        <StackPanel Grid.Row="1" x:Name="PaginationPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,8" Spacing="8">
            <Button x:Name="PrevButton" Content="◄ Previous" Click="OnPrevClick" />
            <TextBlock x:Name="PageText" VerticalAlignment="Center" FontWeight="SemiBold" />
            <Button x:Name="NextButton" Content="Next ►" Click="OnNextClick" />
        </StackPanel>
    </Grid>
</UserControl>
