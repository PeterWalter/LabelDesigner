<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LabelDesigner.App.Views.DesignerCanvasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LabelDesigner.App.Views"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <canvas:CanvasControl x:Name="Canvas"
    AutomationProperties.Name="Label design canvas"
    AutomationProperties.HelpText="Design surface. Use keyboard shortcuts: Ctrl+Z Undo, Ctrl+Y Redo, Ctrl+C Copy, Ctrl+V Paste, Delete Remove, Escape Cancel."
    IsTabStop="True"
    Draw="OnDraw"
    PointerMoved="OnPointerMoved"
    PointerPressed="OnPointerPressed"
    PointerExited="OnPointerExited"
    PointerReleased="OnPointerReleased">
        <canvas:CanvasControl.ContextFlyout>
            <MenuFlyout>
                <MenuFlyoutItem Text="Cut" Command="{Binding Designer.CutSelectedCommand}"/>
                <MenuFlyoutItem Text="Copy" Command="{Binding Designer.CopySelectedCommand}"/>
                <MenuFlyoutItem Text="Paste" Command="{Binding Designer.PasteElementCommand}"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Delete" Command="{Binding Designer.DeleteSelectedCommand}"/>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Bring to Front" Command="{Binding Designer.BringToFrontCommand}"/>
                <MenuFlyoutItem Text="Send to Back" Command="{Binding Designer.SendToBackCommand}"/>
            </MenuFlyout>
        </canvas:CanvasControl.ContextFlyout>
    </canvas:CanvasControl>
</UserControl>
